apiVersion: v2
name: 2060-ts-template
description: Helm chart for the 2060-io TypeScript project template
type: application
# version / appVersion are overwritten at publish time by the CD pipeline
# (helm package --version --app-version). These are just sane defaults.
version: 0.1.0
appVersion: "0.1.0"
