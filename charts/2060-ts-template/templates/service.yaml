{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "2060-ts-template.fullname" . }}
  labels:
    {{- include "2060-ts-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "2060-ts-template.selectorLabels" . | nindent 4 }}
{{- end }}
